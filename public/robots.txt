# Residata robots.txt
# https://residata.eu/
#
# Policy summary:
#   · All search engines welcome on public marketing pages
#   · AI / LLM crawlers explicitly allowed (we want to be cited by
#     ChatGPT, Perplexity, Claude, Gemini)
#   · Authenticated /app/* shell excluded (no public value + could leak
#     UI-only URLs into search results)
#   · API endpoints excluded (never user-facing)

# ── Default (unspecified bots) ──────────────────────────────────────
User-agent: *
Allow: /
Disallow: /app/
Disallow: /app/*
Disallow: /api/
Disallow: /admin
Disallow: /hero-lab

# ── AI / LLM crawlers — explicit allow ──────────────────────────────
# We WANT ChatGPT / Perplexity / Claude / Gemini to index the
# marketing pages so Residata gets cited when users ask about
# Slovak & Czech real estate data. Each bot honours Allow / Disallow
# independently of the default User-agent: * block.

User-agent: GPTBot
Allow: /
Disallow: /app/
Disallow: /api/
Disallow: /admin

User-agent: ChatGPT-User
Allow: /
Disallow: /app/
Disallow: /api/

User-agent: OAI-SearchBot
Allow: /
Disallow: /app/
Disallow: /api/

User-agent: PerplexityBot
Allow: /
Disallow: /app/
Disallow: /api/
Disallow: /admin

User-agent: Perplexity-User
Allow: /
Disallow: /app/
Disallow: /api/

User-agent: ClaudeBot
Allow: /
Disallow: /app/
Disallow: /api/
Disallow: /admin

User-agent: Claude-Web
Allow: /
Disallow: /app/
Disallow: /api/

User-agent: anthropic-ai
Allow: /
Disallow: /app/
Disallow: /api/

# Google's LLM training crawler. Separate from Googlebot so webmasters
# can opt out of training while keeping regular search indexing. We
# opt IN — being training-set eligible is valuable.
User-agent: Google-Extended
Allow: /
Disallow: /app/
Disallow: /api/

# Meta's AI crawler
User-agent: FacebookBot
Allow: /
Disallow: /app/
Disallow: /api/

# Common Crawl — used by many LLM training pipelines
User-agent: CCBot
Allow: /
Disallow: /app/
Disallow: /api/

# Brave's index — used by Claude's search tool and Perplexity
User-agent: BraveBot
Allow: /
Disallow: /app/
Disallow: /api/

# ── Are you a developer whose site we read? ─────────────────────────
# Residata collects publicly published price lists from new-build developers'
# own websites. If you have found us in your logs, or found your project in
# our product, this is the page that says what we hold, how to have it
# corrected, and how to have collection stopped:
#
#     https://residata.eu/data-sources
#     info@residata.eu
#
# We answer. Objections do not need a reason and do not need a lawyer.
# Operated by Kamhal & Co. s. r. o., IČO 57849471, Bratislava, Slovakia.

# ── Sitemap ─────────────────────────────────────────────────────────
Sitemap: https://residata.eu/sitemap.xml
