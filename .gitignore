# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build-time artifact: scripts/generate-static-content.mjs writes this
# JSON snapshot of live data so the Vite plugin can inject the same
# numbers into index.html during the same build. Regenerated each build.
scripts/.build-data.json

# Vercel CLI local link (project/org ids)
.vercel
